kind: DaemonSet
metadata:
  name: longhorn-manager
  namespace: longhorn-system
spec:
  template:
    spec:
     volumes:
      - name: longhorn
        hostPath:
          path: /mnt/storage/longhorn/data
